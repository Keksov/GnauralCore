# Test Sine440 Parity Baseline

Date: 2026-06-01
Status: historical precursor; artifact hashes and first live Audacity capture refreshed 2026-06-03
Fixture: `c:\projects\KKMindWave\SpectrumCore\cli\build\x64\test_sine440.wav`
Fixture SHA256: `48EC56822558957D9922DAB94BA9F4324C66B9B24B6D4CED8F860B9B42CDC4DA`

Canonical normalized R5A replacement:
- `c:\projects\KKMindWave\GnauralCore\spectrum\research\comparison\sine440_1s_mono_44k`

## Scope

This is the first comparison pack built from the current FFTW worker baseline after:

- native-static FFTW closure;
- log-frequency tile projection;
- shared magnitude / phase / unwrapped-phase storage in one analysis session.

This fixture is the original local worker probe file and is now retained as the pre-R5A comparison narrative. New automation should use the canonical `sine440_1s_mono_44k` pack for the normalized sine fixture path.

## SpectrumCore FFTW Worker Baseline

Worker:
- `c:\projects\KKMindWave\SpectrumCore\cli\build\x64\SpectrumCoreFftwWorkerProbe.exe`

Magnitude-mode observations:
- `open-analysis` succeeds with `binding="native-static"`.
- `point-query` at `timeSec=0.50`, `frequencyHz=440.0` resolves to `frameIndex=41`, `binIndex=20`, `binHz=430.664063`.
- Dominant point magnitude is `0.442239`, `db=-7.086859`, `normalized=0.940943`.
- `area-query` over `0.45..0.55 s` and `400..500 Hz` reports `maxMagnitude=0.442242` at `frameIndex=44`, `binIndex=20`.
- `get-tile` with `fscale=log` returns explicit `binFrequenciesHz` from `80` to `4000` Hz.

Phase-mode observations:
- `data=phase` session validates without a second analysis path.
- `point-query` at the sine peak returns `value=2.580945`, `phase=2.580945`, `unwrappedPhase=2.580945` while keeping magnitude fields unchanged.
- `area-query` keeps the same peak bin selection as magnitude mode and adds phase-oriented values on top.

Unwrapped-phase observations:
- `data=uphase` session validates on the same analysis store.
- `point-query` at the sine peak still resolves to the same frame/bin pair.
- `get-tile` diverges from wrapped phase away from the dominant band, which is expected from frequency-axis phase unwrapping.

Reference transcript:
- `c:\projects\KKMindWave\GnauralCore\spectrum\research\fftw\worker-probe-validation-2026-06-01.md`

Analysis-coupled AVTX preview observations:
- After `open-analysis` on the same fixture, `mdct-edit-preview` with `analysisId="sine"`, `timeSec=0.50`, and `sampleCount=8` resolves to `frameIndex=41`, `frameTimeSec=0.499229`, and `sampleStart=22012`.
- The resulting MDCT coefficients are `[3.306162,0.993523,-0.693905,-0.577425]` and the preview samples are `[0.123444,0.041199,-0.041199,-0.123444,-1.778259,-1.785278,-1.785278,-1.778259]`.
- Repeating the same request with `gain=0.0` zeroes the edited coefficients and preview samples, which confirms that the AVTX edit-preview path is now attached to the real FFTW analysis-time mapping on this fixture.

Analysis-range AVTX preview observations:
- `mdct-edit-preview-range` with `analysisId="sine"`, `timeStartSec=0.49`, `timeEndSec=0.51`, and `sampleCount=8` resolves to `frameStart=40`, `frameEnd=42`, and `itemCount=3`.
- Aggregate range stats are `maxPeakAbs=1.953156` and `meanRms=1.075802`.
- Frame `40` preview resolves to `sampleStart=21500`, coefficients `[1.599715,0.307243,-0.258463,-0.200844]`, `peakAbs=0.820831`, `rms=0.587434`.
- Frame `41` preview resolves to `sampleStart=22012`, coefficients `[3.306162,0.993523,-0.693905,-0.577425]`, `peakAbs=1.785278`, `rms=1.261582`.
- Frame `42` preview resolves to `sampleStart=22524`, coefficients `[3.537474,1.236496,-0.819768,-0.696404]`, `peakAbs=1.953156`, `rms=1.378389`.
- Repeating the same range request with `gain=0.0` returns zeroed `editedCoefficients`, zeroed `previewSamples`, `maxPeakAbs=0.000000`, and `meanRms=0.000000` for all three items.

## FFmpeg Reference Pack

Saved under:
- `c:\projects\KKMindWave\GnauralCore\spectrum\research\ffmpeg\test_sine440`

Available artifacts:
- `showspectrum-fullframe-magnitude.png`
- `showspectrum-fullframe-phase.png`
- `showspectrum-fullframe-uphase.png`
- matching `*.log` files
- `ffmpeg-version.txt`
- `input-sha256.txt`

Important mismatch note:
- The installed `ffmpeg.exe` exposes `overlap` and `data` on `showspectrum`, but not on `showspectrumpic`.
- Failed `showspectrumpic-*.log` captures are intentionally kept as evidence of that runtime surface mismatch.
- Because of that mismatch, the live parity pack currently uses `showspectrum:slide=fullframe` instead of `showspectrumpic` for magnitude/phase/uphase reference rasters.

## Audacity Reference Pack

Current state:
- first live capture saved; scripting path works through `mod-script-pipe`

Validated workflow:
- Workspace-relative `SpectrumCore/lib/build/audacity-win64/install/bin/Audacity4.exe` is used as the Audacity runtime for captures.
- `mod-script-pipe.dll` is enabled for the active runtime, and the named pipes `\\.\pipe\ToSrvPipe` and `\\.\pipe\FromSrvPipe` are present after launch.
- Vendored `pipeclient.py` works through the live pipes when run with `python -X utf8`.
- `SetPreference` can stage the legacy `/Spectrum/*` keys, but `Reload=1` resets the live spectrogram values back to defaults.
- A full Audacity process restart after staging `/Spectrum/*` preserves the desired tuple in `audacity.cfg`: `ScaleType=1`, `MinFreq=80`, `MaxFreq=4000`, `Gain=0`, `Range=120`, `FrequencyGain=0`, `FFTSize=2048`, `WindowType=3`, `Algorithm=0`.
- After restart, `Import2` loads the fixture and `SetTrack: Display=Spectrogram SpecPrefs=1 Height=300` succeeds.
- `Help: Command=Screenshot Format=JSON` returns `Command not found`, so the saved artifact uses an OS-level `PrintWindow` capture instead of a built-in scripting screenshot.

Saved capture root:
- `c:\projects\KKMindWave\GnauralCore\spectrum\research\audacity\test_sine440`

Available artifacts:
- `README.md`
- `audacity-window-2026-06-03-printwindow.png`

Live observations:
- The captured Audacity ruler is log-spaced (`100, 150, 200, 300, 390, 500, 700, 1000, 2000, 3000`), which is consistent with the requested logarithmic scale.
- The dominant tone is a stable bright horizontal band centered near `440 Hz` for the full `0..1 s` duration.
- The `0.45..0.55 s` / `400..500 Hz` region contains a narrow cyan-white core at the dominant band.
- Strong vertical flares appear at `0.00 s` and `1.00 s`, and energy above roughly `1.5 kHz` remains comparatively dark.
- Current Audacity source resolves the remaining semantics: modern `colorGainDb` maps directly to legacy `/Spectrum/Gain`, modern `colorRangeDb` maps directly to legacy `/Spectrum/Range`, and the AU3 painter normalizes with `(db + range + gain) / range`.
- That formula means the displayed upper color limit is `-Gain dB`, so the requested `upper limit 0 dB` target maps to `Gain=0` plus `Range=120`.
- A restart-based live comparison capture with `/Spectrum/Gain=1` produced a slightly brighter render than the saved `Gain=0` parity capture (`AvgLuma 0.387844 -> 0.390389` over a broad spectrogram ROI), which is consistent with the same mapping.

## Artifact Hashes Refreshed 2026-06-03

Comparison pack:
- self-hash intentionally omitted here because an in-file SHA256 pin for the manifest would change the file contents and invalidate itself

FFmpeg reference pack:
- `README.md`: `536E26F90019D6685EE25133AAF29BDFAAE797936C6EC5DC7EAF8245BF94A002`
- `ffmpeg-version.txt`: `D7EC79159F60EA06C41E6EA1412D98B591AFD0CFD909F175F21DDB0E541B2900`
- `input-sha256.txt`: `0F41C66057AF55675C7FECEE07B092425C76E8A9FF7EFCFBA6BCAC5EEC432C88`
- `showspectrum-fullframe-magnitude.log`: `7F51100D7B6F2E6020FA9B7372FE5C0E402F1E3C211F4C13020BE83DF49F17F1`
- `showspectrum-fullframe-magnitude.png`: `FEE6548F40424C3C90C8162F97CB0785CCD90D33F4D8E1468865D1A111B971E1`
- `showspectrum-fullframe-phase.log`: `265D86EA015D80EBAAC3A8D66CCA38CCB60E6B7480C599D6F1429E2B926667F2`
- `showspectrum-fullframe-phase.png`: `0E4E108924C89FFAFCE624FC4DCE00D36EE084F1862E34BDDF99D7D46E5BF54E`
- `showspectrum-fullframe-uphase.log`: `13A0730D0DDFAAD8203903390530B24FC130A0E71C0207F2B48F3B8D3DEA98DF`
- `showspectrum-fullframe-uphase.png`: `C7F700834C2BC6BDAD18078C0B4D63D67AB24E5A75B6BC8C3FF648C221E7930D`
- `showspectrumpic-magnitude.log`: `8AE980CEADA63D679EA5B8108539204159F3DF7C6817911A4EA36E1534EE44C8`
- `showspectrumpic-phase.log`: `2ED82C124E98E6438EB500E73859C48D1D572F821FF41B32ED49FB9DA9574D5F`
- `showspectrumpic-uphase.log`: `B364029898691F7DEF0FDBB8C5E45BACDD5088F3F9E6C25B87824755AD8F1034`

Audacity live pack:
- `audacity-window-2026-06-03-printwindow.png`: `48A38B9A11D697F19B632F077B45A43FAD50B0CC9FF51DC85912A9DF88316CA9`
- `audacity-window-2026-06-03-gain1-printwindow.png`: `8E383D484E7C8D4876D0AC3BCB89BC58DB3D0CC71CCFDF80B8F7773BC321BCA8`

## Current Comparison Status

- FFTW worker baseline: ready
- FFmpeg live raster references: ready
- Audacity capture: first live spectrogram capture saved; repeatable configuration currently requires staging `/Spectrum/*` values and a full Audacity restart
- Classification: `partially-comparable` now that live Audacity capture and gain semantics are both pinned down; the remaining open issue is the scripted no-restart settings path plus broader fixture coverage
