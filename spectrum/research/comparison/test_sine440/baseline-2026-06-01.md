# Test Sine440 Parity Baseline

Date: 2026-06-01
Status: started
Fixture: `c:\projects\KKMindWave\SpectrumCore\cli\build\x64\test_sine440.wav`
Fixture SHA256: `48EC56822558957D9922DAB94BA9F4324C66B9B24B6D4CED8F860B9B42CDC4DA`

## Scope

This is the first comparison pack built from the current FFTW worker baseline after:

- native-static FFTW closure;
- log-frequency tile projection;
- shared magnitude / phase / unwrapped-phase storage in one analysis session.

This fixture is still the local worker probe file, not yet the final renamed R5A fixture pack.

## SpectrumCore FFTW Worker Baseline

Worker:
- `c:\projects\KKMindWave\SpectrumCore\cli\build\x64\SpectrumCoreFftwWorkerProbe.exe`

Magnitude-mode observations:
- `open-analysis` succeeds with `binding="native-static"`.
- `point-query` at `timeSec=0.50`, `frequencyHz=440.0` resolves to `frameIndex=41`, `binIndex=20`, `binHz=430.664063`.
- Dominant point magnitude is `0.442239`, `db=-7.086859`, `normalized=0.940943`.
- `area-query` over `0.45..0.55 s` and `400..500 Hz` reports `maxMagnitude=0.442242` at `frameIndex=44`, `binIndex=20`.
- `get-tile` with `fscale=log` returns explicit `binFrequenciesHz` from `80` to `4000` Hz.

Phase-mode observations:
- `data=phase` session validates without a second analysis path.
- `point-query` at the sine peak returns `value=2.580945`, `phase=2.580945`, `unwrappedPhase=2.580945` while keeping magnitude fields unchanged.
- `area-query` keeps the same peak bin selection as magnitude mode and adds phase-oriented values on top.

Unwrapped-phase observations:
- `data=uphase` session validates on the same analysis store.
- `point-query` at the sine peak still resolves to the same frame/bin pair.
- `get-tile` diverges from wrapped phase away from the dominant band, which is expected from frequency-axis phase unwrapping.

Reference transcript:
- `c:\projects\KKMindWave\GnauralCore\spectrum\research\fftw\worker-probe-validation-2026-06-01.md`

## FFmpeg Reference Pack

Saved under:
- `c:\projects\KKMindWave\GnauralCore\spectrum\research\ffmpeg\test_sine440`

Available artifacts:
- `showspectrum-fullframe-magnitude.png`
- `showspectrum-fullframe-phase.png`
- `showspectrum-fullframe-uphase.png`
- matching `*.log` files
- `ffmpeg-version.txt`
- `input-sha256.txt`

Important mismatch note:
- The installed `ffmpeg.exe` exposes `overlap` and `data` on `showspectrum`, but not on `showspectrumpic`.
- Failed `showspectrumpic-*.log` captures are intentionally kept as evidence of that runtime surface mismatch.
- Because of that mismatch, the live parity pack currently uses `showspectrum:slide=fullframe` instead of `showspectrumpic` for magnitude/phase/uphase reference rasters.

## Audacity Reference Pack

Current state:
- pending

Reason:
- `audacity` executable was not found on `PATH` in the current environment.

Planned capture root:
- `c:\projects\KKMindWave\GnauralCore\spectrum\research\audacity\test_sine440`

## Current Comparison Status

- FFTW worker baseline: ready
- FFmpeg live raster references: ready
- Audacity capture: blocked by missing executable in this environment
- Classification: `not-comparable-yet` until Audacity evidence exists and the exact settings tuple is replayed there
