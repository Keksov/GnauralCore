{
  "schemaVersion": 1,
  "plan": "spectrogram-ui-plan",
  "planDoc": "docs/spectrogram-ui/spectrogram-ui-plan.md",
  "title": "Spectrogram UI (Audacity-style) on the SpectrumCore worker",
  "status": "active",
  "createdAt": "2026-06-25",
  "updatedAt": "2026-06-28",
  "decisions": [
    { "id": "DU1", "text": "Architecture: LIVE worker over WebSocket. The bun server spawns/manages a long-lived SpectrumCoreFftwWorkerProbe and proxies JSON; the UI requests tiles on demand by (timeStart,timeEnd,zoom) over the overview pyramid. Reuses the worker's W2/W3 (pyramid + budgeted cache)." },
    { "id": "DU2", "text": "Code location: UI in GnauralCore/ui (source of truth; MindWaveCore re-exports). Server integration in GnauralCore/server (audio domain) wired through MindWaveCore/server (bun, the runtime host). Plan + ledger live here in GnauralCore/docs/spectrogram-ui/." },
    { "id": "DU3", "text": "Worker binary: DEV-ONLY for now. The server locates SpectrumCoreFftwWorkerProbe.exe via config/ENV (run manually in dev); packaging/distribution deferred to a later step." },
    { "id": "DU4", "text": "The UI settings panel must surface the FULL SpectrumCore capability set (post Audacity-parity): fscale lin/log/mel/bark/erb/period; data magnitude/phase/uphase/reassign/pitch; scale lin/sqrt/cbrt/log/4thrt/5thrt; window + zero-padding; the full win_func list incl. gauss25/35/45; gain/frequencyGain/drange/limit/saturation; start/stop; mode combined/separate; channel; palette + color gain. Coverage is an acceptance criterion (U4.3)." },
    { "id": "DU5", "text": "Render split: fetch LINEAR-magnitude tiles from the worker and apply scale/gain/drange/limit/saturation/palette in the renderer (live, no re-analysis). Re-analysis (worker reconfigure) is needed only for window/hop/overlap/win_func/data/fscale/zero-padding/mode/channel/start/stop. Finalized in U4.2." },
    { "id": "DU6", "text": "Time slider: reuse the existing schedule-player time slider/scrubber IF it provides the needed seek + zoom/pan for spectrogram mode; otherwise adapt or build one styled to match. U3.2 includes the evaluation. (GnauralTransportControls.vue is play/pause/export only, not a scrubber.)" },
    { "id": "DU7", "text": "Process: plan doc + JSON ledger; per-step atomic commits prefixed with the step id; NO push (origin left to the user); pause at phase boundaries for review." },
    { "id": "DU8", "text": "Gates: vue-tsc typecheck + lint; bun contract tests for the server worker-bridge (spawn on a fixture; assert tile/point/area shapes; reconfigure; crash-restart) as the PRIMARY automated gate; vitest unit tests for composable/store logic; visual parity vs Audacity/ffmpeg references is manual / non-blocking." },
    { "id": "DU9", "text": "Prerequisite met: the SpectrumCore Audacity full-parity backend (../../../SpectrumCore/spec/audacity-parity-plan.md) is complete; this plan consumes it." }
  ],
  "steps": [
    {
      "id": "U0.1",
      "phase": 0,
      "title": "Plan & ledger",
      "status": "done",
      "dependsOn": [],
      "artifacts": ["docs/spectrogram-ui/spectrogram-ui-plan.md", "docs/spectrogram-ui/spectrogram-ui-progress.json"],
      "verify": "ledger JSON parses; dependsOn DAG acyclic; plan + ledger committed",
      "startedAt": "2026-06-25T00:00:00Z",
      "finishedAt": "2026-06-25T00:00:00Z",
      "blockedReason": null,
      "evidence": [
        "Authored the Spectrogram UI plan + ledger after exploring the stack (Quasar/Vue3 UI in GnauralCore/ui re-exported into MindWave; bun server with WS handlers; existing placeholder SpectrogramView.vue computing Goertzel client-side; SpectrumCore worker exposes open-analysis/get-tile(time,zoom)/point-query/area-query/export-matrix).",
        "DAG acyclic: U0.1 -> U0.2 -> U1.1 -> U1.2 -> U1.3 -> {U2.1->U2.2->U2.3, plus U3/U4/U5 chains}.",
        "Committed as U0.1 (single commit, no push)."
      ]
    },
    {
      "id": "U0.2",
      "phase": 0,
      "title": "WS spectrogram protocol + bridge skeleton + smoke harness",
      "status": "done",
      "dependsOn": ["U0.1"],
      "artifacts": ["SharedPasCore/ts/spectrogram-protocol.ts", "GnauralCore/server/protocol.ts", "GnauralCore/server/spectrogram-bridge.ts", "GnauralCore/server/spectrogram-bridge.test.ts"],
      "verify": "additive WS message types defined (TS); bridge skeleton spawns the worker on a fixture, opens an analysis, requests one tile, asserts shape; bun smoke test green",
      "startedAt": "2026-06-28T21:30:00Z", "finishedAt": "2026-06-28T21:42:05Z", "blockedReason": null,
      "evidence": [
        "Additive WS protocol in SharedPasCore/ts/spectrogram-protocol.ts (no existing type touched): camelCase discriminated unions SpectrogramClientMessage (open/reconfigure/get-tile/point-query/area-query/close) + SpectrogramServerMessage (opened/reconfigured/tile/point/area/closed/error) + payload shapes (SpectrogramAnalysisInfo/Tile/TileFrame/PointResult/AreaResult), mirroring the worker contract. Re-exported via GnauralCore/server/protocol.ts.",
        "GnauralCore/server/spectrogram-bridge.ts skeleton: resolveSpectrogramWorkerExe (SPECTRUMCORE_WORKER_EXE env, else dev fallback ../../SpectrumCore/build/win64/SpectrumCoreFftwWorkerProbe.exe, DU3); spawnSpectrogramWorker (Bun.spawn, NDJSON line reader, FIFO req/resp correlation -- U1.1 will add id-correlation/timeout/restart); smokeOpenAndGetTile (open-analysis -> get-tile -> close, always closes).",
        "bun smoke spectrogram-bridge.test.ts: spawns the real worker on tests/fixtures/sine440_1s_mono_44k.wav, asserts open shape (sampleRate/frameCount/binCount) + tile shape (frames.length==emittedFrameCount, binFrequenciesHz.length==binCount, frame bins.length==binCount). `bun test` -> 2 pass / 0 fail / 14 expect (251ms). skipIf(exe||fixture missing) for portability (DU3 dev-only).",
        "Gates: bun smoke green; tsc --noEmit (MindWave tsconfig, which includes GnauralCore/server) -> exit 0, 0 errors. Stale SpectrumCore cli/spec/ doc links repointed to spec/ after the SpectrumCore restructure.",
        "Two repos (DU2): SharedPasCore commit (protocol types) + GnauralCore commit (bridge/test/re-export/plan+ledger). No push."
      ]
    },
    {
      "id": "U1.1",
      "phase": 1,
      "title": "Worker process manager (spawn / NDJSON / lifecycle)",
      "status": "done",
      "dependsOn": ["U0.2"],
      "artifacts": ["GnauralCore/server/spectrogram-bridge.ts", "GnauralCore/server/spectrogram-worker-manager.test.ts"],
      "verify": "Bun.spawn of the worker (exe path from config/ENV); newline-delimited JSON framing; request/response correlation; timeout + crash-restart; clean shutdown. Unit/bun test for framing + restart",
      "startedAt": "2026-06-28T21:45:00Z", "finishedAt": "2026-06-28T22:05:00Z", "blockedReason": null,
      "evidence": [
        "SpectrogramWorkerManager added to spectrogram-bridge.ts: lazy (re)spawn, per-request timeout (default 15s), crash detection via worker.exited -> drop current -> lazy respawn, restart-loop guard (maxConsecutiveRestarts default 5, reset on a healthy response), restart()/shutdown(). Correlation is FIFO (worker echoes cmd, carries no id) so a timeout POISONS the worker (kill + respawn) to avoid desync; documented. No transparent retry (analysis state is per-process; U1.3 re-opens after a restart).",
        "Low-level spawnSpectrogramWorker hardened: exposes exited:Promise<number>; always drains stderr (backpressure-safe) with optional onStderr sink; fails pending on stdout close AND on child.exited; shared NDJSON line reader (drainLines).",
        "Injectable spawn factory (options.spawn) enables deterministic lifecycle tests without a process. spectrogram-worker-manager.test.ts: timeout->poison+!isRunning, crash->auto-respawn(spawnCount), restart()->fresh worker, restart-loop guard->'exceeded N consecutive restarts', shutdown->sends throw; plus real-process FIFO concurrent-ping framing + real restart (skipIf exe missing, DU3).",
        "Gates: bun test 9 pass / 0 fail / 40 expect (223ms, incl. real worker); tsc --noEmit (MindWave tsconfig) exit 0, 0 errors."
      ]
    },
    {
      "id": "U1.2",
      "phase": 1,
      "title": "Audio source resolution (WAV for the worker)",
      "status": "done",
      "dependsOn": ["U1.1"],
      "artifacts": ["GnauralCore/server/spectrogram-audio-source.ts", "GnauralCore/server/spectrogram-audio-source.test.ts"],
      "verify": "resolve the server-side WAV path for audio.selectedPath; convert non-WAV to a temp WAV via the existing export pipeline; cache + cleanup. Test: open-analysis succeeds for a non-WAV source",
      "startedAt": "2026-06-28T22:10:00Z", "finishedAt": "2026-06-28T22:30:00Z", "blockedReason": null,
      "evidence": [
        "Established: the SpectrumCoreFftwWorkerProbe reads WAV only (SpectrumCoreWavReader; no uos/flac in the probe). The 'existing export pipeline' is the Gnaural render (gnaural-session.ts: `<gnauralExe> <input> -o <tempWav>`); resolveGnauralExecutablePath() (GNAURAL_EXE env / default) supplies exe+cwd.",
        "New SpectrogramAudioSource (spectrogram-audio-source.ts): acquire(filePath, fileKind) -> SpectrogramWavHandle{ wavPath, rendered, release() }. wav -> pass-through (no temp); gnaural -> render to mkdtemp temp WAV (decoupled from the stateful gnaural-session, one Bun.spawn); flac -> explicit 'not yet supported' error (worker is WAV-only; needs worker-side uos decode or a server flac->wav step -- recommended as a SpectrumCore follow-on so there is a single decode path).",
        "Refcounted cache keyed by (kind, resolvedPath, mtime): concurrent opens of the same source share ONE render; the temp WAV is deleted only when the LAST holder releases (kept for the analysis lifetime since the worker may re-read the source WAV, e.g. mdct-edit-preview). dispose() removes all temp renders on shutdown. Injectable renderGnaural + statFile for deterministic tests.",
        "spectrogram-audio-source.test.ts: wav pass-through (no delete), flac rejected, gnaural render->temp->cleanup-on-release, single-render-for-concurrent-holders + delete-on-last-release, and end-to-end 'open-analysis succeeds for a rendered non-WAV source' against the real worker (stubbed render copies the sine440 fixture). bun all spectrogram tests 14 pass / 0 fail / 58 expect; tsc 0 errors.",
        "Validation entrypoint stays resolveAllowedAudioFilePath (audio-file-utils.ts): the WS session (U1.3) resolves+authorizes the path, then SpectrogramAudioSource.acquire produces the worker WAV."
      ]
    },
    {
      "id": "U1.3",
      "phase": 1,
      "title": "WS endpoints (open / reconfigure / get-tile / point / area / close)",
      "status": "done",
      "dependsOn": ["U1.2"],
      "artifacts": ["GnauralCore/server/spectrogram-session.ts", "GnauralCore/server/spectrogram-session.test.ts", "MindWaveCore/server/ui-ws-handler.ts", "GnauralCore/server/index.ts", "SharedPasCore/ts/spectrogram-protocol.ts"],
      "verify": "per-connection session routes UI WS messages to the worker and streams responses; backpressure-safe. bun contract test (open -> get-tile -> point-query -> area-query -> reconfigure -> close) green",
      "startedAt": "2026-06-28T22:35:00Z", "finishedAt": "2026-06-28T23:05:00Z", "blockedReason": null,
      "evidence": [
        "SpectrogramSession (spectrogram-session.ts): per-connection router mapping SpectrogramClientMessage -> SpectrogramServerMessage over SpectrogramWorkerManager + SpectrogramAudioSource. One analysis/session: open resolves+acquires the WAV then open-analysis; reconfigure = close+reopen on the same source with merged params (worker sets params at open); get-tile/point/area forwarded; close closes + releases. Calls serialized per session (promise queue) so state transitions stay ordered; handle() never rejects (failures -> spectrogram:error). Field mapping winFunc->win_func, durationS->durationSec. dispose() closes analysis, releases source, shuts owned deps.",
        "Transport wiring (MindWaveCore/server/ui-ws-handler.ts): per-socket SpectrogramSession map; spectrogram messages routed BEFORE isBrowserMessage (additive protocol, outside ServerEvent) via isSpectrogramClientMessage; resolveSource authorizes the open filePath via resolveAllowedAudioFilePath(getAudioSettings()); responses sent with JSON.stringify; session disposed on socket close. Additive: spectrogram-protocol.ts gains filePath? on open + isSpectrogramClientMessage guard; GnauralCore/server barrel exports the bridge/audio-source/session.",
        "Contract test (spectrogram-session.test.ts) on the REAL worker: open(wav fixture) -> get-tile -> point-query -> area-query -> reconfigure(window 1024 -> windowSize 1024) -> close -> (post-close query errors); plus a no-worker error path (get-tile before open -> spectrogram:error). bun all spectrogram tests 16 pass / 0 fail / 82 expect; tsc --noEmit (MindWave incl. GnauralCore/server) exit 0, 0 errors.",
        "Phase 1 (server worker bridge) complete. Next U2.1 (useSpectrogram composable)."
      ]
    },
    {
      "id": "U2.1",
      "phase": 2,
      "title": "useSpectrogram composable (tile fetch + client cache)",
      "status": "done",
      "dependsOn": ["U1.3"],
      "artifacts": ["GnauralCore/ui/composables/use-spectrogram.ts", "GnauralCore/ui/composables/spectrogram-tiles.ts", "GnauralCore/ui/composables/spectrogram-tiles.test.ts", "GnauralCore/ui/plugin.ts", "MindWaveCore/ui/services/ws.ts", "SharedPasCore/ts/protocol.ts"],
      "verify": "opens an analysis over WS; requests tiles for the visible (time-range, zoom); client-side tile cache; debounced refetch on view change; cancellation. unit for the cache/refetch logic",
      "startedAt": "2026-06-29T13:00:00Z", "finishedAt": "2026-06-29T13:35:00Z", "blockedReason": null,
      "evidence": [
        "Transport: spectrogram WS types are now reachable in the UI via @protocol (added `export * from ./spectrogram-protocol` to SharedPasCore/ts/protocol.ts; removed the now-redundant re-export in GnauralCore/server/protocol.ts). GnauralWsService (plugin.ts) + the host wsService (MindWave ui/services/ws.ts) gain sendSpectrogram(client msg) + onSpectrogram(handler) (the latter filters dispatch by the 'spectrogram:' type prefix and consumes).",
        "Pure, Vue-free tiling/cache core in spectrogram-tiles.ts: tileKey, planVisibleTiles (fixed 256-frame aligned chunks over the visible time-range+zoom, clamped to frameCount), SpectrogramTileCache (LRU). bun test spectrogram-tiles.test.ts -> 9 pass / 29 expect (key stability, window coverage, last-tile clamp, zoom carry, empty/out-of-range, LRU eviction + MRU refresh).",
        "use-spectrogram.ts composable: open/reconfigure (control req/resp by requestId; cache cleared on (re)analysis) + setView(timeRange,zoom,viewBinCount) with debounced refetch (default 80ms), client LRU tile cache, and cancellation via a monotonic viewSeq -- stale tile responses are still cached but never re-assemble the current view. Reactive analysis/tiles/loading/error; onScopeDispose cleanup.",
        "Gates: vue-tsc clean for the new code (the one remaining vue-tsc error is a pre-existing, unrelated BodyMonitorCore EegSignalStyleListPanel typing issue; excluded *.test.ts from the UI tsconfig since those are bun tests). Server tsc 0 errors; bun spectrogram suites (server + ui tiles) 25 pass / 0 fail."
      ]
    },
    {
      "id": "U2.2",
      "phase": 2,
      "title": "Canvas tile renderer (replace the Goertzel placeholder)",
      "status": "done",
      "dependsOn": ["U2.1"],
      "artifacts": ["GnauralCore/ui/components/SpectrogramView.vue", "GnauralCore/ui/composables/spectrogram-render.ts", "GnauralCore/ui/composables/spectrogram-render.test.ts", "GnauralCore/ui/pages/AudioPage.vue"],
      "verify": "draw worker tiles (float -> palette -> ImageData) on canvas; frequency axis honours fscale via binFrequenciesHz; replaces the client-side Goertzel path; renders a fixture end-to-end",
      "startedAt": "2026-06-29T13:45:00Z", "finishedAt": "2026-06-29T14:10:00Z", "blockedReason": null,
      "evidence": [
        "SpectrogramView.vue rewritten to a worker-backed canvas renderer: prop filePath; useSpectrogram open(filePath, window 2048/hop 512) -> setView(full range, zoom=chooseZoom(frameCount, canvasPx), viewBinCount=clamp canvasHeight) -> draw tiles. Each tile -> tileToImage RGBA -> offscreen canvas -> drawImage scaled to its [frameStart,frameStart+frameCount) x-slice. The old client-side Goertzel/AudioBuffer path is removed. fscale-correct by construction (worker returns binFrequenciesHz-mapped display bins; rows drawn in order, top=highest freq). Re-opens on filePath change, re-zooms on resize, closes on unmount.",
        "Pure render core spectrogram-render.ts (DOM-free): magnitudeToIntensity (linear -> dB+drange, gain), tileToImage (frames x bins -> RGBA, top row = highest freq), chooseZoom (2^z pooling for frames/columns). bun test spectrogram-render.test.ts -> 9 pass / 21 expect (0 dB->1, -drange->0, -60 dB->0.5, gain +6 dB, image dims/orientation, missing-bins safety, zoom ratios).",
        "AudioPage.vue: <spectrogram-view> now bound to :file-path=audio.displayFilePath (was :audio-buffer); the store's existing loading/error/empty gating is kept.",
        "Gates: vue-tsc clean for the new code (only the pre-existing unrelated BodyMonitorCore EegSignalStyleListPanel error remains); bun render test 9/9. End-to-end visual render is manual/non-blocking (DU8); axis labels + time ruler are U3.1."
      ]
    },
    {
      "id": "U2.3",
      "phase": 2,
      "title": "Palettes + client-side value transform",
      "status": "done",
      "dependsOn": ["U2.2"],
      "artifacts": ["GnauralCore/ui/composables/spectrogram-render.ts", "GnauralCore/ui/composables/spectrogram-render.test.ts", "GnauralCore/ui/components/SpectrogramView.vue"],
      "verify": "scale/gain/drange/limit/saturation/palette applied in the renderer on linear tiles (live, no refetch); palettes mirror the backend for parity; updates without re-analysis",
      "startedAt": "2026-06-29T14:20:00Z", "finishedAt": "2026-06-29T14:45:00Z", "blockedReason": null,
      "evidence": [
        "spectrogram-render.ts ported the backend math for parity: magnitudeToScaled mirrors SpectrumCoreAnalysisConfig MagnitudeToConfiguredScaled (ffmpeg get_iscale) -- the full scale family lin/sqrt/cbrt/log/4thrt/5thrt with gain/drange/limit (LOG20=20/ln10, dB+range for log; linear-bound clip + root otherwise); paletteColor + hsvToRgb mirror SpectrumCoreRaster ValueToRgb (intensity=grayscale, rainbow=HSV blue->red), with saturation scaling the rainbow HSV sat (default 1 == backend).",
        "tileToImage now applies scale + palette per pixel on the LINEAR tiles. SpectrogramView gains a `render` prop (Partial<SpectrogramRenderOptions>); a deep watch on it redraws the CACHED tiles with no worker refetch (DU5 render-only). A parent settings panel/store will drive it in U4.1.",
        "bun test spectrogram-render.test.ts -> 12 pass / 29 expect (log dB map incl. gain/-drange/-60dB; scale-family lin/sqrt/cbrt ordering; palette intensity/rainbow blue->green->red + saturation=0 desaturates; tile orientation + rainbow tile + missing-bin safety; chooseZoom).",
        "Gates: vue-tsc clean for the new code (only the pre-existing unrelated BodyMonitorCore error remains). Phase 2 (core rendering) complete; settings UI to drive these is U4.1."
      ]
    },
    {
      "id": "U3.1",
      "phase": 3,
      "title": "Frequency axis + time ruler (chrome)",
      "status": "done",
      "dependsOn": ["U2.2"],
      "artifacts": ["GnauralCore/ui/composables/spectrogram-axes.ts", "GnauralCore/ui/composables/spectrogram-axes.test.ts", "GnauralCore/ui/components/SpectrogramView.vue"],
      "verify": "frequency axis labels honour fscale; time ruler labels; the chrome SpectrumCore intentionally omitted is rendered here",
      "startedAt": "2026-06-29T15:00:00Z", "finishedAt": "2026-06-29T15:25:00Z", "blockedReason": null,
      "evidence": [
        "spectrogram-axes.ts (pure): frequencyAxisTicks samples the tile's binFrequenciesHz (fscale-correct by construction; tick0 = top/highest), timeAxisTicks uses niceStep (1/2/5 x 10^k) over the visible range, formatHz (440 / 4.4k / 12k) + formatTimeSec (2.50s / 1:05). bun test 9/9.",
        "SpectrogramView now reserves an axis margin (left 46 / bottom 18 / top 6 / right 8): the spectrogram draws into the plot rect, frequency labels + ticks render down the left (fscale-aware), time labels + ticks along the bottom. applyView() now sizes columns/viewBinCount to the plot area (not the full canvas).",
        "Gates: vue-tsc clean for the new code (only the pre-existing unrelated BodyMonitorCore error remains); bun axes 9/9. Visual placement is manual/non-blocking (DU8)."
      ]
    },
    {
      "id": "U3.2",
      "phase": 3,
      "title": "Time slider + zoom/pan",
      "status": "done",
      "dependsOn": ["U3.1"],
      "artifacts": ["GnauralCore/ui/composables/spectrogram-viewport.ts", "GnauralCore/ui/composables/spectrogram-viewport.test.ts", "GnauralCore/ui/components/SpectrogramView.vue"],
      "verify": "zoom/pan over time drives get-tile by (timeStart,timeEnd,zoom) across pyramid tiers; smooth at multiple zooms",
      "startedAt": "2026-06-29T15:35:00Z", "finishedAt": "2026-06-29T16:00:00Z", "blockedReason": null,
      "evidence": [
        "DU6 evaluated: there is NO reusable scrubber (GnauralTransportControls is play/pause/export only), so a dedicated viewport was built. Pure spectrogram-viewport.ts: TimeWindow + fullWindow/clampWindow/zoomWindow(anchored)/panWindow/isFullWindow/viewportZoomTier (pyramid tier from the visible frame span via chooseZoom). bun test 9/9.",
        "SpectrogramView now holds a visible `view` window: a toolbar (zoom in/out/fit + a q-range over [0,duration] for pan/window) plus wheel-zoom anchored at the cursor. Tiles + the time ruler are positioned/labelled by the visible window; a watch on `view` calls setView(window, viewportZoomTier(...)) so zoom/pan re-fetch the right pyramid tier (composable debounces + caches + cancels stale). The plot is clipped to its rect.",
        "Gates: vue-tsc clean for the new code (only the pre-existing unrelated BodyMonitorCore error remains); bun viewport 9/9. Smoothness at multiple zooms is manual/non-blocking (DU8)."
      ]
    },
    {
      "id": "U3.3",
      "phase": 3,
      "title": "Playhead sync with audio playback (optional)",
      "status": "done",
      "dependsOn": ["U3.2"],
      "artifacts": ["GnauralCore/ui/components/SpectrogramView.vue", "GnauralCore/ui/composables/spectrogram-viewport.ts", "GnauralCore/ui/pages/AudioPage.vue"],
      "verify": "a playhead overlay tracks the transport position during playback; seeking via click updates playback if wired",
      "startedAt": "2026-06-29T16:10:00Z", "finishedAt": "2026-06-29T16:30:00Z", "blockedReason": null,
      "evidence": [
        "SpectrogramView gains playheadSec + seekable props and a `seek` emit: it draws a cyan playhead line at the transport position when inside the visible window (timeToFraction), redrawn (no refetch) as the position moves; clicking the plot (when seekable) maps x -> time (fractionToTime) and emits seek.",
        "Wired in AudioPage to the existing transport: :playhead-sec=displayedPositionSec, :seekable=canSeek, @seek=handleSeek (the same handler the transport controls use -> audio_seek + optimistic local seek). Transport integration was available, so the optional step is fully wired.",
        "Pure helpers timeToFraction/fractionToTime added to spectrogram-viewport.ts; bun viewport test 11/11. vue-tsc clean for the new code (only the pre-existing unrelated BodyMonitorCore error remains). PHASE 3 (navigation) complete."
      ]
    },
    {
      "id": "U4.1",
      "phase": 4,
      "title": "Full parameter panel (all backend capabilities)",
      "status": "done",
      "dependsOn": ["U2.3"],
      "artifacts": ["GnauralCore/ui/composables/spectrogram-settings.ts", "GnauralCore/ui/composables/spectrogram-settings.test.ts", "GnauralCore/ui/stores/spectrogram.ts", "GnauralCore/ui/components/SpectrogramSettingsPanel.vue", "GnauralCore/ui/components/SpectrogramView.vue", "GnauralCore/ui/composables/spectrogram-render.ts", "GnauralCore/ui/pages/AudioPage.vue"],
      "verify": "panel exposes the full backend capability set bound to a pinia store",
      "startedAt": "2026-06-29T17:00:00Z", "finishedAt": "2026-06-29T17:45:00Z", "blockedReason": null,
      "evidence": [
        "Pure spectrogram-settings.ts: SpectrogramSettings (full DU4 set) + DEFAULT_SPECTROGRAM_SETTINGS + option lists (window sizes, zero-pad, the full win_func list incl. gauss25/35/45, data magnitude/phase/uphase/reassign/pitch, fscale lin/log/mel/bark/erb/period, scale lin/sqrt/cbrt/log/4thrt/5thrt, channel modes, palettes) + mappers toRenderOptions (client) / toAnalysisParams (worker; startHz/stopHz -> start/stop). bun test 4/4.",
        "Pinia store stores/spectrogram.ts (useSpectrogramStore): reactive settings + derived renderOptions/analysisParams + reset(). SpectrogramSettingsPanel.vue binds every option (q-select/q-input/q-slider) to the store, grouped Analysis vs Display.",
        "Wiring: AudioPage passes :analysis=store.analysisParams + :render=store.renderOptions to SpectrogramView (alongside the settings panel). SpectrogramView consumes them: analysis params seed open + a deep watch reconfigures the open source (re-analysis) then re-applies the view; render options are live (no refetch). frequencyGain added to the render path (frequencyGainFactor, per-bin via binFrequenciesHz) so it is functional render-only. bun render test +frequencyGain.",
        "Gates: vue-tsc --noEmit exit 0, 0 errors (UI fully clean after the QMenu fix); bun ui spectrogram suite 46 pass / 0 fail. Apply-semantics polish (debounce/dedup) + persistence are U4.2; coverage matrix + presets are U4.3."
      ]
    },
    {
      "id": "U4.2",
      "phase": 4,
      "title": "Apply semantics (render-only vs reconfigure) + persistence",
      "status": "not-started",
      "dependsOn": ["U4.1"],
      "artifacts": ["GnauralCore/ui/composables/useSpectrogram.ts", "GnauralCore/ui/stores (spectrogram store)"],
      "verify": "render-only params (scale/gain/drange/limit/saturation/palette) update without refetch; reconfigure params (window/hop/overlap/win_func/data/fscale/zero-padding/mode/channel/start/stop) trigger re-analysis; settings persist (localStorage)",
      "startedAt": null, "finishedAt": null, "blockedReason": null, "evidence": []
    },
    {
      "id": "U4.3",
      "phase": 4,
      "title": "Capability-coverage check + Audacity/ffmpeg presets",
      "status": "not-started",
      "dependsOn": ["U4.2"],
      "artifacts": ["GnauralCore/ui (presets)", "docs/spectrogram-ui/capability-coverage.md"],
      "verify": "every SpectrumCore option (DU4) reachable from the UI (coverage matrix doc + a test asserting the option set); Audacity-default and ffmpeg-showspectrumpic-default presets selectable",
      "startedAt": null, "finishedAt": null, "blockedReason": null, "evidence": []
    },
    {
      "id": "U5.1",
      "phase": 5,
      "title": "Hover readout (time / frequency / intensity)",
      "status": "not-started",
      "dependsOn": ["U3.1"],
      "artifacts": ["GnauralCore/ui/components/SpectrogramView.vue", "GnauralCore/ui/composables/useSpectrogram.ts"],
      "verify": "hovering shows time/frequency/intensity via worker point-query (throttled); readout matches the rendered cell",
      "startedAt": null, "finishedAt": null, "blockedReason": null, "evidence": []
    },
    {
      "id": "U5.2",
      "phase": 5,
      "title": "Area selection -> area-query overlay",
      "status": "not-started",
      "dependsOn": ["U5.1"],
      "artifacts": ["GnauralCore/ui/components/SpectrogramView.vue"],
      "verify": "drag-select a time/frequency region -> worker area-query -> peak/mean stats overlay; selection maps correctly under zoom/pan and fscale",
      "startedAt": null, "finishedAt": null, "blockedReason": null, "evidence": []
    },
    {
      "id": "U5.3",
      "phase": 5,
      "title": "i18n / a11y / states / perf + final acceptance",
      "status": "not-started",
      "dependsOn": ["U4.3", "U5.2"],
      "artifacts": ["GnauralCore/ui (i18n, states)", "docs/spectrogram-ui/spectrogram-ui-progress.json"],
      "verify": "i18n strings; a11y (canvas aria, keyboard); loading/empty/error states; perf pass (tile cache sizing, request cancellation); vue-tsc + lint + bun contract + vitest all green; set plan status complete",
      "startedAt": null, "finishedAt": null, "blockedReason": null, "evidence": []
    }
  ],
  "log": [
    { "ts": "2026-06-25T00:00:00Z", "step": "U0.1", "event": "done", "note": "Spectrogram UI plan + ledger created. Architecture: live SpectrumCore worker over WebSocket via the bun server (DU1); UI in GnauralCore/ui re-exported to MindWave (DU2); worker exe dev-only via config/ENV (DU3); UI must surface the full post-Audacity-parity capability set (DU4); render-only vs reconfigure split (DU5). Prerequisite (Audacity full-parity backend) complete." },
    { "ts": "2026-06-28T21:42:05Z", "step": "U0.2", "event": "done", "note": "Additive WS spectrogram protocol (SharedPasCore/ts/spectrogram-protocol.ts, re-exported) + spectrogram-bridge.ts skeleton (spawn worker, NDJSON FIFO, open->get-tile->close) + bun smoke (real worker on sine440 fixture: 2 pass/0 fail/14 expect) + tsc 0 errors. Stale cli/spec/ doc links repointed to spec/ post-restructure. Phase 0 complete; next U1.1 (process manager: id-correlation, timeout, crash-restart)." },
    { "ts": "2026-06-28T22:05:00Z", "step": "U1.1", "event": "done", "note": "SpectrogramWorkerManager: lazy (re)spawn, per-request timeout (poison-on-timeout to keep FIFO sane), crash detection+auto-restart, restart-loop guard, restart()/shutdown(); spawnSpectrogramWorker hardened (exited, stderr drain). Injectable spawn factory -> deterministic lifecycle tests (timeout/crash/restart/guard/shutdown) + real FIFO concurrent-ping & restart. bun 9 pass/0 fail/40 expect; tsc 0 errors. Next U1.2 (audio source resolution)." },
    { "ts": "2026-06-28T22:30:00Z", "step": "U1.2", "event": "done", "note": "SpectrogramAudioSource: wav pass-through; gnaural->temp WAV via the Gnaural render pipeline; flac unsupported (worker WAV-only; needs worker uos decode / server decode). Refcounted cache by (kind,path,mtime), cleanup on last release (temp kept for analysis lifetime), dispose() on shutdown. Injectable render/stat -> deterministic tests + real worker open-analysis on a rendered non-WAV source. bun 14 pass/0 fail; tsc 0 errors. Next U1.3 (WS endpoints + contract test)." },
    { "ts": "2026-06-29T17:45:00Z", "step": "U4.1", "event": "done", "note": "Full settings panel + pinia store: spectrogram-settings.ts (DU4 option lists + render/analysis mappers, bun 4/4), stores/spectrogram.ts, SpectrogramSettingsPanel.vue. AudioPage drives view analysis (reconfigure-on-change) + render (live); frequencyGain added to render path (per-bin). vue-tsc 0 errors (UI fully clean); bun ui suite 46/0. Apply-semantics/persistence = U4.2; coverage/presets = U4.3." },
    { "ts": "2026-06-29T16:30:00Z", "step": "U3.3", "event": "done", "note": "Playhead overlay (timeToFraction) tracks displayedPositionSec; click-to-seek (fractionToTime) -> emit seek -> AudioPage handleSeek (audio_seek). Wired to the existing transport (canSeek). bun viewport 11/11; vue-tsc clean for new code. PHASE 3 (navigation) COMPLETE. Next U4.1 (full settings panel)." },
    { "ts": "2026-06-29T16:00:00Z", "step": "U3.2", "event": "done", "note": "DU6: no reusable scrubber -> built spectrogram-viewport.ts (window zoom/pan/clamp + viewportZoomTier) bun 9/9. SpectrogramView toolbar (zoom in/out/fit + q-range pan) + cursor-anchored wheel-zoom; visible window drives setView across pyramid tiers; tiles + time ruler positioned by the window. vue-tsc clean for new code. Next U3.3 (playhead, optional)." },
    { "ts": "2026-06-29T15:25:00Z", "step": "U3.1", "event": "done", "note": "spectrogram-axes.ts (frequencyAxisTicks from binFrequenciesHz = fscale-correct; timeAxisTicks niceStep; formatHz/formatTimeSec) bun 9/9. SpectrogramView reserves an axis margin and draws freq labels (left) + time ruler (bottom) around the plot rect; applyView sizes to the plot area. vue-tsc clean for new code. Next U3.2 (time slider + zoom/pan)." },
    { "ts": "2026-06-29T14:45:00Z", "step": "U2.3", "event": "done", "note": "Ported backend get_iscale (scale family lin/sqrt/cbrt/log/4thrt/5thrt + gain/drange/limit) + ValueToRgb palettes (intensity/rainbow + saturation) into spectrogram-render.ts for parity; tileToImage applies them per-pixel on linear tiles; SpectrogramView `render` prop -> deep-watch redraw with NO refetch (DU5). bun render 12/12; vue-tsc clean for new code. PHASE 2 (core rendering) COMPLETE. Next U3.1 (freq axis + time ruler)." },
    { "ts": "2026-06-29T14:10:00Z", "step": "U2.2", "event": "done", "note": "SpectrogramView.vue rewritten to a worker-backed canvas renderer (useSpectrogram + tileToImage RGBA + drawImage; chooseZoom; fscale-correct via binFrequenciesHz); removed the Goertzel/AudioBuffer path; AudioPage bound to displayFilePath. Pure spectrogram-render.ts (intensity/tileToImage/chooseZoom) bun 9/9. vue-tsc clean for new code; end-to-end visual is manual (DU8). Next U2.3 (palettes + client value transform)." },
    { "ts": "2026-06-29T13:35:00Z", "step": "U2.1", "event": "done", "note": "useSpectrogram composable + pure tiling/LRU core (spectrogram-tiles, bun 9/9) + WS transport (sendSpectrogram/onSpectrogram; spectrogram types via @protocol). open/reconfigure + setView debounced refetch + viewSeq cancellation + client cache. vue-tsc clean for new code (1 pre-existing unrelated BodyMonitor error); server tsc 0; bun 25/25. Next U2.2 (canvas renderer)." },
    { "ts": "2026-06-28T23:05:00Z", "step": "U1.3", "event": "done", "note": "SpectrogramSession (per-connection router: open/reconfigure/get-tile/point/area/close -> worker; serialized; error-as-message) + transport wiring in MindWave ui-ws-handler (per-socket session, routed before isBrowserMessage, resolveAllowedAudioFilePath authorization, dispose on close) + additive protocol (filePath? + isSpectrogramClientMessage guard) + barrel exports. Real-worker contract test (open->tile->point->area->reconfigure->close). bun 16 pass/0 fail/82 expect; tsc 0 errors. PHASE 1 COMPLETE; next U2.1 (useSpectrogram composable)." }
  ]
}
